package com.farmaaishrestaurant.dto;

/**
 * Placeholder — exists only so that unqualified "LocalTime" in this package resolves safely.
 * All DTO fields that previously used this type are now declared with java.time.LocalTime explicitly.
 * This class is intentionally empty and unused at runtime.
 */
public class LocalTime {
    private LocalTime() {}
}
